# Windows Zone.Identifier files (WSL download metadata)
*:Zone.Identifier

# Environment / secrets
.env
.env.local

#don't commit node modules
*node_modules/*

# generated CircleMUD world JSON (rebuild from infrastructure/lib/world via the parser)
week0_explore/preview/data/world/