1. The agent file with referenced files e.g. AGENT.md, @~/docs/*.MD

Observations: 
- Coding harness has real-world data. It asked me if I wanted it to find a real bakery first. 
- Coding harnesses will read local files, not pertaining to the loop.
- The Agent ended up creating temp file to create a socket connection and execute commands
- The agent is building a world to display the visits